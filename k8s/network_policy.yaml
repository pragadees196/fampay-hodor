apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  namespace: hodor-acd
  name: allow-bran-to-hodor
spec:
  podSelector:
    matchLabels:
      app: bran
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: hodor
